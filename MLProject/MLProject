name: adult_income_prediction

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      data_path: {type: str, default: adult_income_dataset_preprocessing.csv}
    command: "python modelling.py --data_path {data_path}"